# Coffee-Shell

## Building with Meson
First setup `build` directory:
```bash
meson setup build
```

Compile project with:
```bash
meson compile -C build
```

## Running
After compiling:
```bash
./build/cofsh
```

